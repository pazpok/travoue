{{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="hidden" name="state" value="{{ state }}">
    <button class="btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
